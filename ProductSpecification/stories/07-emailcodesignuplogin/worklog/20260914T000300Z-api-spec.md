# Spec — api-spec

Started: 2026-09-14T00:03:00Z

Outcome: completed

- Change: создан `endpoints.md` — история потребляет `challenge/start` и `challenge/verify` из истории 01; новых YAML нет, существующие `auth_challenge_start.yaml` / `auth_challenge_verify.yaml` переиспользуются без правок.
- Content: таблица «ответ → экран → реакция фронта» для всех статусов обоих эндпоинтов; поля, которые фронт не читает (`message`, `challengeType`).
- Current state: по `acceptance/tests/backend/auth/` включены start/verify happy-path, 400, 409-отсчёт, 401 на неверный код, регистрация, ведущий ноль; RED — resend (500 вместо 401 на вытесненный код), expired/unknown, terminal. Отмечено, что E2E-стаб моделирует целевое 401.
- Next: `test-spec`.
