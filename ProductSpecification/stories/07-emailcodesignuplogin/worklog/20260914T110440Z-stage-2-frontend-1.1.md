# 1.1 Пользователь открывает приложение, получает код на почту и оказывается внутри — stage-2 frontend implementation lanes

Started: 2026-09-14T11:04:40Z

Outcome: completed

План lane — из `20260914T104536Z-stage-1-frontend-1.1.md` (блок `stage-2-plan`). Stage 1 закоммичен в `eaf6624`.
Замороженные поверхности (read-only): `frontend/src/features/auth/types/**`.

## Манифесты и базлайны

| Lane | Манифест | Baseline |
|---|---|---|
| frontend-logic | `frontend/src/features/auth/logic/**`, `frontend/src/features/auth/__tests__/*.logic.test.ts` | пути отсутствуют |
| frontend-api | `frontend/src/features/auth/api/**`, `frontend/src/features/auth/__tests__/*.api.test.ts` | пути отсутствуют |
| design-alignment | `frontend/src/features/auth/components/**`, `frontend/src/pages/AuthPage.tsx`, `frontend/src/app/App.tsx`, `frontend/src/app/theme.css`, `frontend/src/app/components/ui/**` | `App.tsx` blob `325a2780`, `theme.css` blob `b79981b5`, остальные отсутствуют |

Lane-проверки: сфокусированный `npx vitest run <filter>` + `wc -l`; полный `npm run typecheck` / `npx vitest run` / `npm run build` — на join.

## Линия frontend-api — RED

- Цель: клиент challenge (`api/challenge.api.ts`, `api/unexpected-response.ts`), 19 кейсов в трёх
  файлах `__tests__/*.api.test.ts` (start 9, verify 9, reporter 1). Ключевое: тела запросов точные
  (`challengeType: "EMAIL_CODE"`, `code` строкой с ведущим нулём), 400/409/401/503/500 → исходы
  без `message`, non-JSON и неполные 200 → `unexpected` (fail-closed), таймаут через fake timers →
  `unavailable`, `console.error` ровно с `{path, status}` без PII.
- Предсказание: 19× `Error: Not implemented` (заглушки бросают до HTTP). Факт: 19 failed, то же.
  Сравнение — все YES. Маркер `describe.skip` в трёх файлах; lane-check `vitest run api`: 19 skipped.
- Заметка для GREEN: таймаут строить на `setTimeout` + `AbortController` (fake timers не перехватывают
  `AbortSignal.timeout`).

<!-- frontend-lanes: api=RED manifest=api/**,__tests__/*.api.test.ts baseline=absent result=19-skipped checks=vitest-api -->

## Линия frontend-logic — RED

- Цель: чистая логика auth (`logic/{email,code,countdown}.logic.ts`, `logic/auth.reducer.ts`), 71 кейс
  в четырёх файлах `__tests__/*.logic.test.ts` (email 17, code 22, countdown 13, reducer 19). Ключевое:
  `too-long` считает октеты (254), только ASCII-цифра в ячейку, backspace-переходы, `joinCode` держит
  ведущий ноль, `formatCountdown` `m:ss` с `ceil`/clamp, редьюсер отбрасывает ответы с чужим `requestId`,
  `tick` → `expired` по нулю. Fake timers не нужны — время приходит параметрами.
- Предсказание: 71× `Error: Not implemented`. Факт: 71 failed, то же (`uniq -c`). Сравнение — все YES.
  Маркер `describe.skip` в четырёх файлах; lane-check: 71 skipped.
- Координатор: `auth.reducer.test.ts` переименован в `auth.reducer.logic.test.ts` под шаблон манифеста.

<!-- frontend-lanes: logic=RED manifest=logic/**,__tests__/*.logic.test.ts baseline=absent result=71-skipped checks=vitest-logic -->

## Линия frontend-api — test review

- A:4 P:0 S:0 Se:0 → 5 правок в 2 файлах: проверка `Content-Type: application/json` на happy path
  start/verify; гард `console.error` не вызван в кейсах «лишние поля»; новый кейс 409 без
  `retryAfterSeconds` → `unexpected` (hz-04, fail-closed). Итого 20 кейсов; lane-check: 20 skipped.

<!-- frontend-lanes: api=REVIEWED manifest=api/**,__tests__/*.api.test.ts result=20-skipped checks=vitest-api -->

## Линия frontend-logic — test review

- A:2 P:0 S:0 Se:0 → `toEqual` → `toStrictEqual` в трёх файлах (41 место); +3 кейса защиты от дублей
  запросов в редьюсере (hz-03: `nextRequestId` при ожидающем запросе, поздний `code-verified` без
  ожидающего запроса — no-op). Ветки ошибок Tier 2 намеренно не добавлены. Итого 74 кейса; 74 skipped.

<!-- frontend-lanes: logic=REVIEWED manifest=logic/**,__tests__/*.logic.test.ts result=74-skipped checks=vitest-logic -->

## Линия frontend-api — GREEN

- `api/challenge.api.ts` (92), `api/json-post.ts` (48, транспорт: fetch + `AbortController` +
  глобальный `setTimeout`, любое отклонение fetch → `unavailable`), `api/unexpected-response.ts` (5).
  Маркеры сняты; `vitest run api`: 20 passed; typecheck по файлам линии чистый.

<!-- frontend-lanes: api=GREEN manifest=api/**,__tests__/*.api.test.ts result=20-passed checks=vitest-api,tsc-lane -->

## Линия frontend-logic — GREEN

- `logic/email.logic.ts` (61, октеты через `TextEncoder`), `countdown.logic.ts` (15), `code.logic.ts` (82),
  `auth.reducer.ts` (135, тотален по `AuthAction`: ветки Tier 2 реализованы минимально по спеке;
  `tick` не переводит в `expired` во время `submitting`). Маркеры сняты; `vitest run logic`: 74 passed;
  полный vitest 95 passed; typecheck чистый.

<!-- frontend-lanes: logic=GREEN manifest=logic/**,__tests__/*.logic.test.ts result=74-passed checks=vitest-logic,tsc -->

## Линия frontend-api — refactor

- M:1 T:2(NO ACTION) D:1 → 2 правки в `challenge.api.ts`: `parseSession` симметричен `parseStarted`;
  `DEFAULT_TIMEOUT_MS` типизирован `ChallengeRequestTimeoutMs`. Остальные файлы без изменений.
  `vitest run api`: 20 passed; typecheck по файлам линии чистый. Линия закрыта.

<!-- frontend-lanes: api=PASS manifest=api/**,__tests__/*.api.test.ts result=20-passed checks=vitest-api,tsc-lane -->

## Линия frontend-logic — refactor

- M:3 T:0 D:0 → `auth.reducer.ts`: фабрики `emailRoute`/`codeRoute` (11 замен), билдер `startedCode`;
  `code.logic.ts`: `selectCellsTone`/`selectConfirmButton` вместо вложенных тернарников. Сплит редьюсера
  отклонён (циклический импорт между экранами). `vitest run logic`: 74 passed; typecheck чистый. Линия закрыта.

<!-- frontend-lanes: logic=PASS manifest=logic/**,__tests__/*.logic.test.ts result=74-passed checks=vitest-logic,tsc-lane -->

## Линия design-alignment — результат

- Компоненты: `AuthFlow` (useReducer + один `setInterval(tick)` с cleanup, вызовы клиента), `AuthShell`,
  `BrandStage` (доп. файл в манифесте), `EmailScreen`, `CodeScreen`, `CodeCells`, `ScreenMessage`,
  `SignedInScreen`; `pages/AuthPage.tsx`; `app/App.tsx`; `app/theme.css` (85); ui `primary-button`,
  `secondary-link`. Все 17 testid контракта на месте; `app-title` — логотип в `BrandStage` (экраны почты
  и кода). Humble Objects: без валидации/fetch, без `challengeId`/токенов/`message` на экране.
- `/align-design` PASS; `/design-review` PASS (плейсхолдеров нет); `/refactor`: 2 правки (мёртвые токены
  `theme.css`, таблицы сообщений в `ScreenMessage`), 4 NO ACTION; verify-only `/align-design` PASS.
- Coverage (report-only): `@vitest/coverage-v8` не установлен — ручная карта; непокрытые ветки —
  сценарии Tier 2 (2.2, 2.4, 2.5, 3.3, 3.4, 4.1, 4.3), dead code нет. Новых чекбоксов не добавлено
  (finding-admission: все пробелы уже названы существующими Tier 2 сценариями).
- Отклонения: `.auth-card` max-width 430px (центрирование по ui-conventions); текст кулдауна по тестам
  2.4/4.3, а не `m:ss` мокапов 06/11; `SecondaryLinkProps` без пропа иконки — иконка выбирается по
  `testId` (пробел frozen-типа, чинить в `types/**` при Tier 2); `.app-title`-правило удалено из
  `theme.css` как мёртвое (`--color-telegram` вернётся с историей 06).

<!-- frontend-lanes: design=PASS manifest=components/**,pages/AuthPage.tsx,app/App.tsx,app/theme.css,app/components/ui/** baseline=App.tsx@325a2780,theme.css@b79981b5 result=align-PASS,design-review-PASS checks=typecheck,vitest-95,build -->

## Join

- Проверки: `npm run typecheck` PASS; `npx vitest run` 8 файлов, 95 passed; `npm run build` OK;
  `./gradlew compileTestJava` OK. Все чекпоинты `frontend-lanes` — PASS (logic, api, design).
- Манифесты соблюдены: в worktree нет путей вне трёх манифестов (кроме незакоммиченного
  `infrastructure/agent-progress.log`). Ничего из lane не стейджилось до join.
- Tests: vitest 95 passed, 0 failed (logic 74, api 20, app 1).
