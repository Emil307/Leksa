# 1.1 Пользователь открывает приложение, получает код на почту и оказывается внутри — stage-1 frontend acceptance RED + interface design

Started: 2026-09-14T10:45:36Z

Outcome: completed

## Подготовка координатора (до диспетчеризации)

- Инфраструктура стаба: `E2E_API_STUB_PORT` (8173 + REPO_INDEX) в `frontend/infrastructure/.env`
  и `setup-ports.sh`; `stub_url()` в `_env.sh`; `test-e2e.sh` экспортирует порт; Gradle
  пробрасывает его в тесты; `junit-platform.properties` включает параллельный запуск.
- Dev-сервер запущен через `run-frontend.sh` с `VITE_API_URL=http://localhost:8174`.

## Контракт data-testid (общий для обеих линий)

| Экран | testid | Элемент |
|---|---|---|
| почта | `email-screen` | контейнер экрана |
| почта | `email-title` | заголовок «Войти или создать аккаунт» |
| почта | `email-input` | поле адреса (autofocus) |
| почта | `request-code-button` | «Получить код» |
| код | `code-screen` | контейнер экрана |
| код | `code-email` | введённый адрес |
| код | `code-countdown` | отсчёт `m:ss` до истечения |
| код | `code-cell-0` … `code-cell-5` | шесть ячеек кода (input) |
| код | `confirm-button` | «Подтвердить» |
| код | `resend-button` | «Отправить новый код» |
| код | `change-email-link` | «Изменить адрес» |
| вошёл | `signed-in-screen` | контейнер заглушки «вошёл» |
| вошёл | `signed-in-title` | заголовок «Вы вошли» |
| все | `app-root` | корневой контейнер (уже есть), текст страницы для проверки отсутствия секретов |

## Манифесты Stage 1 (непересекающиеся)

- **Selenium RED (red-agent, layer=selenium)**: `frontend/e2e/src/test/java/com/uwords/e2e/tests/auth/**`,
  `frontend/e2e/src/test/java/com/uwords/e2e/statements/auth/**`,
  `frontend/e2e/src/test/java/com/uwords/e2e/stub/**`. Baseline: пути отсутствуют.
- **Interface design (/design-preview, frontend concurrent mode)**: `frontend/src/features/auth/types/**`.
  Baseline: путь отсутствует.
- Координатор: `frontend/e2e/build.gradle.kts`, `frontend/e2e/src/test/resources/junit-platform.properties`,
  `frontend/infrastructure/scripts/{_env,setup-ports,test-e2e}.sh`.

## Линия Selenium RED — результат

- Цель: `com.uwords.e2e.tests.auth.EmailCodeEntryTest` (группа из 3 кейсов), Statements
  `EmailCodeEntryStatements` (страница) и `AuthStubStatements` (стаб), стаб `stub/` (JDK HttpServer,
  singleton на `E2E_API_STUB_PORT`, CORS, журнал запросов по email, детерминированный код).
- Roster → Then: `should_show_email_screen_on_open` (экран почты: заголовок, пустое поле в фокусе,
  «Получить код» недоступна, нет ячеек/отсчёта/повтора); `should_request_code_and_show_code_screen`
  (ровно один `start`, экран кода с адресом, отсчёт 4:50–5:00, шесть пустых ячеек + фокус,
  «Подтвердить» недоступна, «Отправить новый код»/«Изменить адрес» доступны);
  `should_confirm_code_and_sign_in` (ровно один `verify` с `challengeId` и `code` строкой, экран
  «вошёл», на странице нет токенов/session id/challengeId).
- Предсказание: 3× `TimeoutException` (visibility `email-screen` / `email-input`, 10 с).
  Факт: 3 failed, те же типы и сообщения. Сравнение — все YES. Маркер `@Disabled("RED: …")` на классе.
- Координатор после join: 409-код стаба выровнен со спецификацией (`CONFLICT` вместо
  `CHALLENGE_COOLDOWN`, ошибка была в задании линии). `@Description` остаётся на методах
  (`com.uwords.e2e.Description` имеет `@Target(METHOD)`); заголовок группы — `@DisplayName`.

## Линия interface design — результат

- Заморожено: `frontend/src/features/auth/types/` — `challenge-api`, `session`, `challenge-client`,
  `email-screen`, `code-screen`, `auth-screen`, `auth-logic`, `components` (+ `index.ts`), только
  типы; typecheck зелёный. `AuthTestId` фиксирует контракт data-testid (+ `email-message`,
  `code-message`). Исходы клиента не несут `message` конверта; view-модели не содержат
  `challengeId`/токенов.
- Hazard-скан драфта дизайна: 8 групп + синтез; hz-03 — отклонена блоком (вне высоты). GAP свёрнуты
  в интерфейсы: hz-02 таймаут fetch (`timeoutMs`), hz-04 дрейф тел (unexpected, не исключение),
  hz-05 fail-open verify (unexpected ≠ signed-in), hz-06 длина email (`too-long`, 254 октета),
  hz-06 утечка таймера (cleanup в `useEffect`, гард — review lane), hz-07 наблюдаемость без PII
  (`ReportUnexpectedResponse({path,status})`). Два GAP оформлены как Tier 3: UI 5.4 (hz-05,
  экранирование адреса) и UI 5.5 (hz-08, потеря введённого кода при перезагрузке).
- Расхождения для Stage 2: tech-binding кладёт клиент в `logic/{feature}.api.ts`, план — `api/**`
  (следуем плану); текст кулдауна на экране почты — по тесту 2.4, а не `0:42` из мокапа 06.

<!-- stage-2-plan:
frontend-logic: unit=чистая логика auth; writes=[src/features/auth/logic/{email.logic,code.logic,countdown.logic,auth.reducer}.ts: validateEmail/build*Request/select*View, ячейки, parseExpiresAt/computeSecondsLeft/formatCountdown, authReducer/createInitialAuthState/nextRequestId; src/features/auth/__tests__/*.logic.test.ts]; frozen-surfaces=[src/features/auth/types/**]
frontend-api: unit=challenge API client; writes=[src/features/auth/api/challenge.api.ts: createChallengeApiClient/startChallenge/verifyChallenge (fetch, VITE_API_URL, AbortSignal timeout, статус→исход); src/features/auth/api/unexpected-response.ts: reportUnexpectedResponse; src/features/auth/__tests__/*.api.test.ts (MSW 2)]; frozen-surfaces=[src/features/auth/types/**]
design-alignment: unit=экраны auth; writes=[src/features/auth/components/{AuthFlow,AuthShell,EmailScreen,CodeScreen,CodeCells,SignedInScreen,ScreenMessage}.tsx, src/pages/AuthPage.tsx, src/app/App.tsx, src/app/theme.css, src/app/components/ui/{primary-button,secondary-link}.tsx]; mockups=01–13; frozen-surfaces=[src/features/auth/types/**]; imports-by-name=[@/features/auth/logic/*, @/features/auth/api/challenge.api]
-->

## Test review (Selenium RED)

- 4 кластера детекторов: A:7 P:4 S:0 Se:8 → 12 правок в 3 файлах; `compileTestJava` OK, `@Disabled` сохранён.
- Ключевое: точное равенство `RecordedRequest` для start/verify; отсчёт привязан к `expiresAt` стаба
  (±3 с) с проверкой тика; ровно шесть ячеек; подписи кнопок/ссылки по тексту мокапов
  («Получить код», «Подтвердить», «Отправить новый код», «Изменить адрес»); `signed-in-title` = «Вы вошли»;
  тег-селектор `body` заменён на `app-root`.
- Контракт data-testid дополнен `signed-in-title` (и в `AuthTestId`) до заморозки.

## Join

- Проверки: `npm run typecheck` PASS; `npx vitest run` 1 passed; `./gradlew compileTestJava` OK.
- Манифесты соблюдены: RED — только `tests/auth`, `statements/auth`, `stub`; design — только `types/`.
