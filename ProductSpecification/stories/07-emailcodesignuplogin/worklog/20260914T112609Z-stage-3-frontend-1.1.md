# 1.1 Пользователь открывает приложение, получает код на почту и оказывается внутри — stage-3 frontend acceptance GREEN + review

Started: 2026-09-14T11:26:09Z

Outcome: completed

## Предусловия

- Stage 2 закоммичен: `af49e81`; неизменяемый диапазон для review — `eaf6624..af49e81`.
- Dev-сервер (PID из `frontend/infrastructure/run/frontend.pid`) отвечает 200 на `FRONTEND_PORT`,
  запущен с `VITE_API_URL` на стаб (`E2E_API_STUB_PORT`).

## Selenium GREEN (remove-marker-only)

- Единственное изменение теста: удалены `@Disabled("RED: …")` и его импорт в
  `EmailCodeEntryTest.java`. Production-код, Statements, стаб не менялись.
- Первый прогон: 3 failed — `BindException` на порту стаба: порт держал мой же smoke-процесс
  `StubSmoke` из Stage 1 (scratchpad). Остановлен по PID; не дефект кода.
- Повторный прогон `test-e2e.sh 'com.uwords.e2e.tests.auth.EmailCodeEntryTest'`: 3 passed, 0 failed
  (`should_show_email_screen_on_open`, `should_request_code_and_show_code_screen`,
  `should_confirm_code_and_sign_in`), BUILD SUCCESSFUL.
- Полный E2E-сьют `test-e2e.sh`: 4 passed (AppShellTest 1, EmailCodeEntryTest 3), BUILD SUCCESSFUL.

## Demo

- `/demo EmailCodeEntryTest`: видимый Chrome (E2E_HEADLESS=false, окно 1280×800), задержка 1,2 с в
  навигации/вводе/проверках экранов; 3 passed, BUILD SUCCESSFUL. Временные правки
  `AbstractUiTest`/`EmailCodeEntryStatements` откачены, worktree чист.

## Review (agent-review + premortem, диапазон `eaf6624..af49e81`, параллельно с GREEN)

- Чекпоинт координатора: joined-review для Stage 2 коммита `af49e81`; батч потреблён один раз,
  закрытие сценария повторный review не диспатчит.
- **agent-review: CONCERNS** — 1 SAFE (E2E не привязывает `VITE_API_URL` dev-сервера к стабу —
  `test-e2e.sh` не проверяет, куда смотрит запущенный сервер), 4 записи без допуска в план
  (см. триаж), 3 NO_FIX.
- **premortem: CONCERNS** — 2 правдоподобных инцидента: Backspace в заполненной ячейке кода —
  no-op (NEEDS_CYCLE), `test-e2e.sh` не пинит `VITE_API_URL` на стаб (SAFE); 5 NO_FIX.

## Триаж (`triage-and-auto-fix.md`, `finding-admission-test.md`)

### SAFE — применено после join, `review-fix:`

- `run-frontend.sh`: флаг `E2E_STUB=1` экспортирует `VITE_API_URL=$(stub_url)`; PID и адрес API
  пишутся в `run/frontend.pid` и `run/frontend.api-url`, cleanup чистит оба.
- `test-e2e.sh`: после проверки доступности сервера — гард `run/frontend.api-url == stub_url`,
  иначе выход 1 с подсказкой перезапустить `E2E_STUB=1 run-frontend.sh`.
- `frontend/infrastructure/README.md`: строка `test-e2e.sh` описывает запуск через `E2E_STUB=1`.
- Проверка: гард отверг вручную запущенный сервер (ожидаемое сообщение); после
  `stop-frontend.sh` + `E2E_STUB=1 run-frontend.sh` — `EmailCodeEntryTest` 3 passed, BUILD SUCCESSFUL.
  Побочное наблюдение: старый экземпляр `run-frontend.sh` (запущен до правки) снял `frontend.pid`
  своим trap уже после старта нового — гонка stop→trap, восстановлено вручную; новый cleanup
  консистентен, дефекта в правке нет.

### NEEDS_CYCLE — допущено (предложение, ждёт согласия пользователя)

- **P1. Backspace в заполненной ячейке кода ничего не делает.** `CodeCells.tsx` перехватывает
  Backspace только у пустой ячейки; `enterCellDigit` отбрасывает `''`; `clearCellBackwards`
  из компонента недостижима. Нарушает замороженный интерфейс `ClearCellBackwards`
  (`types/auth-logic.ts`) и зелёные тесты логики/reducer. Правило допуска: 1 (наблюдаемо
  пользователем: опечатку нельзя исправить без «Изменить адрес»), 3 (есть обязательство —
  замороженный тип + тесты). Предлагаемый цикл: `<!-- review-origin: boundary -->` блок в Tier 2
  фронтенда — Selenium RED «Backspace очищает ячейку, «Подтвердить» недоступна», затем
  `CodeCells` всегда перехватывает Backspace → `onCellBackspace(index)`.

### NEEDS_CYCLE — не допущено (правило 5: нет обязательства в текущем тире; только запись)

- Кулдаун не тикает / «Отправить новый код» заперта — обязательство Tier 2 (2.4/4.3); риск релиза,
  если Tier 1 уйдёт без Tier 2 — упомянуть при harvest.
- Непарсимый `expiresAt` → `NaN:NaN` — Tier 3 (2.6), продюсер всегда ISO.
- Экран остаётся активным во время pending resend — обязательства нет, Tier 3 4.4 закрыт.
- Автозаполнение `one-time-code` / вставка нескольких цифр отбрасывается в `CodeCells`/
  `enterCellDigit` — обязательства нет, отметили оба агента; решение за пользователем.

### NO_FIX

- Дублирование клиента/таймаута в `AuthPage.tsx` и `challenge.api.ts` — стилистика, отложено.
- Стаб `ChallengeHandler`: кулдаун и выдача не атомарны — тестовый стаб, гонки в кейсах нет.
- Вырожденная схема `requestId` — достаточна для Tier 1.
- Потерянный ответ `start` → 409 до конца кулдауна — по спецификации.
- Отсутствие `VITE_API_URL` бросает при импорте — намеренный fail-fast.
- Ветки Tier 2 (401 сохраняет ячейки и т. п.) — уже в плане.

## Итог

- Selenium GREEN 3/3, полный E2E 4/4, demo 3/3; vitest 95/95 (join Stage 2 без изменений).
- Прогресс: `stage-3` → `[x]`, добавлен чекпоинт `resolve stage-3 cycle proposals` (P1).

## Решение по предложениям (2026-09-14)

- P1 (Backspace в заполненной ячейке) — **отклонено пользователем** («принимаю как есть»):
  код остаётся без изменений, блок цикла не вставлен. Чекпоинт `resolve stage-3 cycle proposals`
  закрыт, следующий шаг — `harvest`.
