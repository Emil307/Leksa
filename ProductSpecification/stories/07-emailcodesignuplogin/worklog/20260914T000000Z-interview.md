# Spec — interview

Started: 2026-09-14T00:00:00Z

Outcome: completed

- Change: история 07 переведена из Backlog в In Progress, создана папка `07-emailcodesignuplogin`, `progress.md` со Spec-секцией, записан `interview.md`.
- Interview: 5 раундов через структурированные вопросы; пользователь подтвердил итоговый файл без правок.
- Key facts: клиент — Telegram Mini App без SDK; email — единый аккаунт; сессия только в памяти; 401 на verify не различается; E2E против стаба API, параллельно, с реальными интервалами.
- Open point: стаб может отдавать короткие `expiresAt`/`retryAfterSeconds` для таймерных E2E — оставлено на решение позже.
- Next: `story` (генерация спецификации).
