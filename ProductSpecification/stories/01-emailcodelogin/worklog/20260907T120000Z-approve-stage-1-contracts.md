# 4.6 Пользователь, чей email записан в базе в другом регистре, не получает второго аккаунта — approve stage-1 contracts

Started: 2026-09-07T12:00:00Z

Outcome: completed

Пользователь одобрил Stage 1 явно: «одобряю stage-1, продолжай».

Одобрение получено после отчёта, в котором были названы: обе полосы Stage 1 и их
коммиты (`bebdbec` поведение, `175f66b` рефакторинг), предсказание и фактический
отказ обоих случаев, дефект `session_issuance.py:55`, решение
`challenge-verify-legacy-email-case-decision.md`, отброшенные находки рефакторинга,
план Stage 2 и две оговорки — обрыв ревизии `0005` на коллизиях в dev-базе после
приёмочного RED-прогона и отсутствие именованного стража у самой ревизии.

Одобрением заморожены контракты Stage 1. В `backend/**` контрактных изменений нет:
домен, use case, порты и REST остаются как есть, поэтому замороженная поверхность —
это существующие сигнатуры, перечисленные в блоке `stage-2-plan` записи
`20260907T112000Z-stage-1-acceptance-red-contract-design.md`.

## Проверка гейта плана Stage 2

Полоса ровно одна — `adapter-storage`, граница `SessionIssuancePort` в реализации
`adapter_storage`. Проверено перед допуском:

- полоса выведена из архитектурной границы, а не из числа файлов;
- манифест `writes` перечисляет все шесть путей с названной дельтой поведения;
- пересечений манифестов нет — полоса одна, `acceptance/**` ей не принадлежит;
- межполосных зависимостей нет по построению;
- `frozen-surfaces` записаны символами и сигнатурами, не файлами целиком: файл
  `session_issuance.py` одновременно несёт замороженные методы и тело, которое
  полоса меняет.

Дробить эту работу на несколько полос было бы нарушением гейта: маппер, модель и
ревизия — соавторы одной границы, и каждая из них входит через публичный
`issue_session`.

Следующий шаг: `stage-2 implementation lanes`.
