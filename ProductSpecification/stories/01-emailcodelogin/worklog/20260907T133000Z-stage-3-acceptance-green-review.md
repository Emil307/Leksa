# Stage 3 — acceptance GREEN (Story 1, сценарий 4.6)

## Блокер и его снятие

Ревизия `0005_auth_users_email_normalized` не накатывалась: её сканер коллизий нашёл
6 групп адресов, различающихся только регистром или окружающими пробелами. Это остатки
строк от собственных acceptance-прогонов RED — суммарно 47 пользователей, 39 сессий,
54 письма в outbox, и ни одной строки, не относящейся к домену `uwords-acceptance.local`.

Санкционированного способа их убрать не существовало: в `infrastructure/scripts/` не было
ничего, что чистит тестовые данные. По правилу инфраструктуры это значит создать IaC-источник,
а не править живую базу руками — добавлен `infrastructure/scripts/clear-acceptance-data.sh`
(коммит b8d7d55), по умолчанию только отчёт, удаление — под `--apply`, в одной транзакции,
сопоставление по `btrim(email)`, чтобы строки с окружающими пробелами тоже попадали в выборку.

После очистки `migrate.sh` прошёл: `alembic current` = `0005_auth_users_email_normalized (head)`,
`uq_t_users_email_normalized` на месте, старый `t_users_email_key` снят.

## Дельта stage-3

Снят только классовый `@pytest.mark.skip(reason=RED_REASON)` и осиротевшие вместе с ним
`RED_REASON` и `import pytest`. Ни строки продакшн-кода, ни строки Statements не изменены.

## Прогон

Полный acceptance: `6 passed, 2 skipped` (пропуски — предсуществующие Tier 2: token refresh,
profile read). Прицельно по классу: оба кейса 4.6 — `2 passed`.

## Ревью

Не запускалось. Блок 4.6 несёт `<!-- review-origin: boundary -->` — он сам порождён находкой
граничного ревью, и его закрывающая граница по правилу глубины пропускает `agent-review`
и `premortem`.

## Осталось названным

Две E501 в `test_challenge_verify_stored_spelling_acceptance.py` (строки 27 и 46) —
предсуществующие, проверено на stash; stage-3 разрешает только снятие маркера, поэтому
не тронуты.
