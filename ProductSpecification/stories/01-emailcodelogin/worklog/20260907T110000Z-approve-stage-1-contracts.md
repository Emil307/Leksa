# 4.2 Неизвестный email регистрируется при верном коде и получает сессию — approve stage-1 contracts

Started: 2026-09-07T11:00:00Z

Outcome: APPROVED — на основании прямого указания пользователя «полностью сделай сценарий Backend
Scenarios 4.2 из Tier 1 истории 1». Отдельного «одобряю контракты» на этот шаг не звучало;
единственное, что зафиксировано в диалоге, — приказ провести сценарий через все стадии.

## Что одобряется

**Ничего нового.** Контракт шва `verify` заморожен целиком ещё при одобрении Stage 1 сценария 4.1
(`worklog/20260903T060000Z-approve-stage-1-contracts.md`); полоса design на 4.2 вернула
`NO_CHANGE` под `backend/`. Одобрять здесь можно только вывод, а не поверхность:

- вывод ALREADY_GREEN — все четыре «Тогда» 4.2 выполняются кодом, поставленным Stage 2 сценария
  4.1, доказательства по строкам в `worklog/20260907T104500Z-stage-1-acceptance-red-contract-design.md`;
- следствие: `stage-2 implementation lanes` помечается `[S]` — реализовывать нечего, и по правилу
  ALREADY_GREEN шаблона `parallel-backend-stages.md` фабриковать полосы под уже работающий код
  запрещено;
- новых портов, адаптеров, миграций и полей ответа не заводится.

## Предпосылка Stage 2

Отсутствует: Stage 2 не запускается.

## Что осталось нерешённым и ждёт пользователя

Два открытых шва из сведения опасностей владельца в плане не получили. Ни один не мешает закрыть
4.2 и ни один не относится к Tier 1, поэтому в план они не вносятся — но и не растворяются:

1. **S3** — запись challenge с ненормализованным ключом порождает вторую личность; страж (повторный
   `Email.of` в `EmailCodeChallengeStrategy.account_for`) не покрыт ни 8.4, ни 4.6, ни границей истории.
2. **S11** — просроченные строки `t_sessions` никем не удаляются; операции жизненного цикла в
   истории нет.

Оба описаны в записи Stage 1. Заводить под них сценарии — решение пользователя, не координатора.

## Проверки

Ничего не исполнялось: это шаг решения. Состояние на входе — `lint-imports` 1 kept / 0 broken,
`pytest backend` 59 passed, приёмка 7 passed / 2 skipped (skip принадлежат историям 4 и 5).
