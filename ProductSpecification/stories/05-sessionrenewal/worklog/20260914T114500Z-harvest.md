# Harvest — Tier 1 → Tier 2

Started: 2026-09-14T11:45:00Z

Outcome: completed

- Контекст: Tier 1 закрыт (`8ce4fbe`). Harvest-набор — 21 сценарий `Tier: 2` из
  `tests/01_API_Tests.md` (7), `06_Integration_Tests.md` (2), `05_Security_Tests.md` (6),
  `04_Infrastructure_Tests.md` (6). Все секции в staged-форме: зелёный как есть → stage-1 `[x]`,
  остальные `[S]` с именем реализующего кода; красный → отключённый тест остаётся стартом цикла.
- Один `red-agent` (layer acceptance) на сценарий, все 21 параллельно; координатор — единственный
  писатель `progress.md`, staging и коммитов. Общие файлы (`conftest.py`, `auth_statements.py`) —
  минимальные правки с перечитыванием перед Edit.
- Baseline: буквальный «до первого коммита истории» (`208683b`) — Python-эпоха до Java-переписывания (Task 2);
  взят `b4e9f8e` (`af4fd9b^`) — Java-приложение перед первой полосой реализации Story 5; текущие зелёные тесты против него.

## Результат полос (21 red-agent, layer acceptance, параллельно)

| Сценарий | Вердикт | Тест-класс | Кейсы (green/red) |
|---|---|---|---|
| API 1.1 | ALREADY_GREEN | `TestRefreshRequestValidationAcceptance` | 3/0 |
| API 1.3 | ALREADY_GREEN | `TestMaxLengthRefreshTokenAcceptance` | 1/0 |
| API 2.2 | ALREADY_GREEN | `TestRotationDeadlinesAcceptance` | 3/0 |
| API 2.3 | ALREADY_GREEN | `TestNeighborSessionIsolationAcceptance` | 1/0 |
| API 3.1 | ALREADY_GREEN | `TestIndistinguishableRefreshRefusalAcceptance` | 2/0 |
| API 3.2 | ALREADY_GREEN | `TestSessionExpiryBoundaryAcceptance` | 3/0 |
| API 3.3 | ALREADY_GREEN | `TestConcurrentRotationAcceptance` | 3/0 |
| Intg 1.2 | ALREADY_GREEN | `TestConditionalWriteSingleWinnerAcceptance` | 2/0 |
| Intg 1.3 | ALREADY_GREEN | `TestLostRotationResponseAcceptance` | 3/0 |
| Sec 1.1 | RED | `TestRotationRequestBoundaryAcceptance` | 4/6 (6 запрещённых полей → 200 вместо 400) |
| Sec 1.3 | ALREADY_GREEN | `TestSqlMetacharacterTokenAcceptance` | 2/0 |
| Sec 2.1 | RED | `TestRefreshTokenSessionIsolationAcceptance` | 1/1 (`userId` в теле → 200 вместо 400) |
| Sec 2.2 | ALREADY_GREEN | `TestSessionRefusalIndistinguishabilityAcceptance` | 2/0 |
| Sec 3.1 | ALREADY_GREEN | `TestAccessTokenAlgorithmAndClaimsAcceptance` | 3/0 |
| Sec 3.2 | ALREADY_GREEN | `TestRotationFailureDisclosureAcceptance` | 3/0 |
| Infra 1.1 | RED | `TestStorageFailureRollbackAcceptance` | 2/1 (отказ хранилища → 500 text/plain вместо 503) |
| Infra 1.2 | INFEASIBLE_AT_ACCEPTANCE | — | отказ подписанта не имеет black-box рычага |
| Infra 1.3 | INFEASIBLE_AT_ACCEPTANCE | — | нужна инъекция отказа портов + перехват журнала |
| Infra 2.1 | RED | `TestTokenConfigurationBootAcceptance` | 6/2 (TTL за Instant.MAX принят при старте) |
| Infra 2.2 | ALREADY_GREEN | `TestTokenRefreshConnectionReleaseAcceptance` | 1/0 |
| Infra 2.3 | RED | `TestTtlFractionAcceptance` | 1/1 (строка хранит floor(now)+TTL, доля секунды отброшена) |

- Все предсказания red-agent'ов совпали с фактом (одно уточнение у Infra 2.1: дробный TTL
  проходит — Spring `FailureAnalysisReporter` печатает origin-плейсхолдер с именем переменной).
- Полный прогон после сборки батча: **173 passed, 30 skipped** (до harvest: 125/19).
- Общие файлы: `conftest.py` (+7 записей `pytest_plugins`), `security_fixtures.py`,
  `infrastructure_fixtures.py`, `session_rotation_fixtures.py`, `auth_client.py`
  (`refresh_session_capturing_outcome`); `connection_release_statements.py` — три assert-метода
  делегированы в новый `pool_release_assertions.py` (2.1 перепроверен, зелёный).
- Коллизия: агенты API 3.3 и Intg 1.2 оба начали с `test_concurrent_rotation_acceptance.py`;
  Intg 1.2 переименовал свои артефакты в `conditional_write_single_winner_*`. Оба класса
  проверяют один инвариант (ровно один победитель) — перекрытие осознанное, оба оставлены.

## Baseline (earned-red)

- Приложение `b4e9f8e` (jar из worktree в scratchpad, порт из `infrastructure/.env`); текущие
  тесты 19 классов против него: **40 failed, 6 passed, 11 skipped**. У baseline маршрут
  `/api/v1/auth/token/refresh` — Stage-1 скелет `AuthTokenController.refresh` →
  `UnsupportedOperationException` → 500: все 40 кейсов работающего приложения RED → **earned**
  для всех 14 ALREADY_GREEN сценариев и зелёных кейсов RED-сценариев.
- 6 passed — JVM-проба `TestTokenConfigurationBootAcceptance` запускает *текущий* jar, а не
  baseline; её зелёные кейсы реализует `AuthTokenProperties` (`@Positive`/`@NotBlank`),
  существующий с Java-порта (`c147613`, Task 2). Сценарий Infra 2.1 всё равно RED (2 кейса).
- Teardown: baseline PID остановлен, штатный backend поднят `run-backend.sh`. Удалений
  инертных тестов нет.

## Решения и пометки для Tier 2

- Infra 1.2 / 1.3: acceptance-полоса невозможна — stage-1 этих секций ограничен contract design,
  RED уходит в usecase (`RefreshSessionTokensService` с fake `AccessTokenIssuerPort`) и
  `rest/tests` (`GlobalExceptionHandler`, перехват logback). Секции оставлены `[ ]` с пометкой.
- Sec 1.1 и Sec 2.1 красны по одной причине: `SessionRefreshRequestDto`
  `@JsonIgnoreProperties(ignoreUnknown = true)` против allowlist из ADR/`endpoints.md`; один
  REST-фикс закроет оба.
- Infra 2.3 (красный кейс) противоречит ADR `token-rotation-contracts-decision.md`
  (`SessionPolicy.truncate` → floor до секунды) и окну Tier 1 Integration 1.1. Решение —
  спека или ADR — на approve stage-1 этой секции, не в harvest.
- Клаузы о журналах (Sec 1.3, Sec 3.2, Sec 1.1) на acceptance не наблюдаемы (stdout backend —
  сессионный файл, объявленного пути лога нет); владелец — `rest/tests` на
  `GlobalExceptionHandler` (прецедент Story 4, 4.2).

## Refactor (детекторы M/D/T → refactor-agent) — `54a086c`

- 19 рефакторингов, 41 файл (+388/−627), полный прогон **173 passed, 30 skipped** (без изменений).
  Пять клиентов-двойников (`refresh_refusal_client`, `refresh_request_validation_client`,
  `session_owner_override_client`, `session_rotation_race_client`) и два DTO
  (`rotation_outcome_dto`, `wire_refusal_dto`) свёрнуты в `AuthClient` (`refresh_session_capturing_refusal`,
  `refresh_session_naming_owner`, `refresh_with_json_body/raw_body/without_a_body`) и
  `SessionRefusalDto(http_status, body, raw_text)`; `ApplicationClient._nested_object` поднят;
  общие проверки — `wire_contract.assert_validation_refusal/assert_error_envelope_shape`,
  `access_token.segment_of/header_of/expiry_of_token`, `stored_session_row.assert_stored_row_unchanged`,
  `session_rotation_consistency_statements.assert_row_stores_rotation`; `BoundaryAttempt` разделён на
  `HostileRefresh` + `BoundaryAttempt`; одна `RotationRace` на оба гоночных сценария.
- Не применено: A40 (ожидаемые значения из runtime-данных в `rotation_deadlines`/`ttl_fraction`) —
  admission не пройден, Stage 3 «plausible but unproven»; 16 Statements вне диапазона harvest
  (`hostile_email`, `sql_injection`, …) по-прежнему объявляют wire-константы локально — именованный
  follow-up без чекбокса.

## Boundary review (`b3523a1`, agent-review ‖ premortem) и triage

- **agent-review: CONCERNS** (5 SAFE, 5 NO_FIX). **premortem: CONCERNS** (2 SAFE, 1 NEEDS_CYCLE,
  1 NEEDS_CLARIFICATION, 6 REMOTE/NO_FIX). Оба независимо назвали Infra 2.3 и Sec 3.2.
- SAFE, применено в `review-fix:`:
  1. Infra 2.3 vs ADR (agent-review #1 = premortem #4). Premortem пометил NEEDS_CLARIFICATION, но
     escalation rule 1 не пройдено — направление выводимо из ADR `SessionPolicy.truncate` + API 2.2
     «с точностью до секунды» + зелёный `rotation_deadlines_statements`; квиз не проводился.
     Спека §2.3 и Notes переписаны на «целая секунда + 17, оба срока отбрасывают долю одинаково»;
     красный кейс → `test_session_deadline_is_the_whole_second_of_the_rotation_plus_the_lifetime` +
     `assert_both_deadlines_drop_the_fraction_of_the_same_second` (`expires_at − REFRESH_TTL ==
     exp − ACCESS_TTL == floor`), skip снят → **2 passed**. Секция закрыта staged-формой `[x]/[S]`.
  2. Sec 3.2 закрыта при наблюдаемом нарушении семейством «отказ хранилища» (agent-review #2).
     Добавлен отключённый `test_should_hide_the_sentinel_when_the_storage_fails` (живая сессия +
     `SessionRowLock.hold`); docstring возвращён к Gherkin спеки минус клаузы о журналах.
     Проверка RED без маркера: предсказано `«отказ хранилища» must answer a JSON object, got
     'Internal Server Error'`; факт — `… got None` (text/plain не парсится в JSON); точка та же,
     причина та же — маркер ссылается на факт. Секция → harvest-RED, зеленеет циклом Infra 1.1 (503).
  3. progress.md не помечал четыре RED-секции «тест уже написан» (agent-review #3) — пометки на
     Sec 2.1, Infra 1.1, Infra 2.1 (Infra 2.3 закрыта п. 1, Sec 3.2 — п. 2).
  4. Дублирующие DTO (agent-review #4) — уже снято `54a086c` (`RotationOutcomeDto` удалён).
  5. Локальные fixtures в тест-файлах (agent-review #5) — `rotation_request_boundary_statements`,
     `rotation_failure_disclosure_statements` → `fixtures/security_fixtures.py`; `session_row_lock`,
     `storage_failure_rollback_statements` → `fixtures/infrastructure_fixtures.py`.
  6. Клаузы о журналах Sec 3.2 / Sec 1.3 без гварда (premortem #1) — `RefreshTokenTest`
     (`toString() == "<redacted>"`, первый тест `backend/domain`) и
     `AuthTokenControllerLogRedactionTest` + `RefreshLogRedactionStatements` (`ListAppender`,
     сентинел через 400/401/нечитаемое тело — ни одна строка, включая throwable chain, не несёт
     сентинел): **4 passed** (`:backend:domain:test`, `:backend:adapters:rest:test`).
  7. API 2.3 +60 s на второй логин (premortem #2) — сосед той же учётки открывается через
     `auth_database.open_session`, `SecondLoginStatements` из сценария убран; `wait_for_the_second_after`
     сохраняет строгое `>` по сроку. Targeted-прогон пяти затронутых классов: **12 passed, 8 skipped**.
- NEEDS_CYCLE, admission пройден (premortem #3): log-injection через имя неизвестного поля после
  фикса allowlist (`GlobalExceptionHandler.java:52` логирует `failure.getMessage()`). Обязательство —
  Sec 1.1 «поддельный маркер не появляется в журналах». Не вставляется в план: гвард входит в
  contract design уже открытой секции Sec 1.1 (per-DTO `ignoreUnknown=false`, константный текст лога,
  rest-тест с `ListAppender` и телом `{"refreshToken":"t","a\r\nforged=true":1}`).
- Dropped (NO_FIX/REMOTE, одной строкой каждое):
  - agent-review: Infra 2.1 «точный max TTL принимается» гоночно на acceptance — место в unit-тесте
    `AuthTokenProperties` на stage-2; API 2.3 строгое `>` без ожидания — снято п. 7; API 3.2 «ровно в
    срок» покрыт `shouldReturnNoneWhenTheSessionExpiresExactlyNow`; API 3.3 / Intg 1.2 `gather` не барьер —
    атомарность у `shouldRefuseRotationWhenThePresentedTokenWasAlreadyRotated`; baseline 40/40 без JVM-проб
    Infra 2.1 — уже зафиксировано выше.
  - premortem: гонка API 3.3 / Intg 1.2 REMOTE (тот же гвард); API 3.2 REMOTE; Infra 1.1 связь
    `DB_STATEMENT_TIMEOUT_SECONDS < REQUEST_TIMEOUT_SECONDS` неявная — цена гварда выше инцидента;
    Infra 2.1 пробы поверх jar на диске — прецедент Story 1; Infra 2.2 без пути отказа хранилища — зона
    Infra 1.1; `handleUnexpected` пишет stack trace `PersistenceException` без значения токена.
- stories.md: Tests остаётся 14/21 (Infra 2.3 +1, Sec 3.2 −1), 67%.
