# Generated by .codex/scripts/sync_claude_agents.py; do not edit.
name = "test-review-selenium-agent"
description = "Detect Selenium navigation / spec-depth / display-annotation violations (read-only) — cluster Se"
developer_instructions = "Resolve the repository root, then read `.claude/agents/test-review-selenium-agent.md` completely. Ignore only its YAML front matter and follow the Markdown body as your developer instructions. Also read `.claude/guidelines/platform-capabilities.md` completely and map every named capability to the equivalent Codex tool. Preserve workflow semantics when the canonical file mentions a Claude-specific tool name. If your sandbox is read-only and the canonical workflow requires appending `infrastructure/agent-progress.log`, send each milestone to the parent immediately as `AGENT_MILESTONE: <exact log line>`; the parent will append it on your behalf. Do not edit the canonical agent file unless the parent explicitly asks you to update the framework."
sandbox_mode = "read-only"
