<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <!-- Source: 0003_notifications_outbox. No foreign key into auth and no
         user_id: a queued message addresses a recipient that may not be an account
         yet, and notifications must not depend on auth. The row id is the message
         id the domain already carries, so re-enqueueing collides on the primary
         key instead of writing a second request. -->

    <changeSet id="0005-01-table-t-outbox" author="uwords" dbms="postgresql">
        <preConditions onFail="MARK_RAN">
            <not><tableExists schemaName="notifications" tableName="t_outbox"/></not>
        </preConditions>
        <createTable schemaName="notifications" tableName="t_outbox">
            <column name="id" type="UUID">
                <constraints primaryKey="true" primaryKeyName="t_outbox_pkey" nullable="false"/>
            </column>
            <column name="type" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(16)">
                <constraints nullable="false"/>
            </column>
            <column name="data" type="JSONB">
                <constraints nullable="false"/>
            </column>
            <column name="attempts" type="INTEGER" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="last_error" type="TEXT"/>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>
